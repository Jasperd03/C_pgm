#include<stdio.h>
void main()
{
    int rows,col,n=4;
    for(rows=1;rows<=n;rows++)
    {
        for(col=1;col<=rows;col++)
        {
            printf("*");
        }
        printf("\n");
    }
}

Output:
*
**
***
****
